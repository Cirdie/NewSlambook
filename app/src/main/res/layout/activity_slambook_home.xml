<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/main"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    tools:context=".SlambookHomeActivity"
    android:background="@drawable/background_minimalist"
    >

    <!-- RelativeLayout to position child views inside it -->
    <RelativeLayout
        android:layout_width="match_parent"
        android:layout_height="match_parent">

        <!-- Fragment container view for navigation -->
        <androidx.fragment.app.FragmentContainerView
            android:id="@+id/nav_host_fragment"
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            app:navGraph="@navigation/slambook_nav_graph"
            android:name="androidx.navigation.fragment.NavHostFragment" />

        <!-- BottomNavigationView positioned at the bottom of the screen -->
        <com.google.android.material.bottomnavigation.BottomNavigationView
            android:id="@+id/bottom_navigation"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_alignParentBottom="true"
            android:background="@drawable/bottom_nav_background"
        app:menu="@menu/slambook_bottom_menu"
        app:labelVisibilityMode="selected"
            />

    </RelativeLayout>

</androidx.constraintlayout.widget.ConstraintLayout>
